// Public contractor landing page: pai.app/contractor/{id}
// Reuses the existing contractor profile screen, which reads `id`
// from route params (path segment here, query param when opened
// in-app via /contractor-profile).
export { default } from '../contractor-profile';
